<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Notes - Notes App</title>
  <style>body {
      font-size: 1.15rem;
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background-color: #fafafa;
      color: #202020;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }
    .menu-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.75rem 1rem;
      background-color: #ffffff;
      border-bottom: 1px solid #e6e6e6;
      position: relative;
      z-index: 1000;
    }
    .menu-icon,
.menu-dots,
.sidebar-toggle {
  background: none;
  border: none;
  outline: none;
  cursor: pointer;
  padding: 0.5rem 0.75rem;
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background-color 0.2s;
}
    
    }
    .app-container {
      display: flex;
      flex: 1;
      overflow: hidden;
      position: relative;
    }
    .sidebar {
      width: 260px;
      background-color: #fff;
      border-right: 1px solid #e6e6e6;
      transition: transform 0.3s ease;
      z-index: 3000;
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      transform: translateX(-100%);
      display: flex;
      flex-direction: column;
    }
    .sidebar.open {
      transform: translateX(0);
    }
    .sidebar-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.75rem 1rem;
      border-bottom: 1px solid #e6e6e6;
    }
    .sidebar h3 {
      font-size: 0.85rem;
      font-weight: 600;
      color: #888;
      margin: 1rem 0 1rem 0.25rem;
    }
    .sidebar ul {
      list-style: none;
      padding-left: 0;
      margin: 0;
    }
    .sidebar ul li {
      margin: 0.25rem 0;
    }
    .sidebar ul li a {
      display: block;
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      color: #333;
      text-decoration: none;
      font-size: 0.95rem;
    }
    .sidebar ul li a:hover {
      background-color: #f0f0f0;
    }
    main {
      flex: 1;
      padding: 2rem;
      overflow-y: auto;
      background-color: #fafafa;
    }
    .dropdown-menu {
      position: absolute;
      top: 3.5rem;
      right: 1rem;
      background-color: white;
      border: 1px solid #ddd;
      border-radius: 6px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      display: none;
      min-width: 140px;
      z-index: 1000;
    }
    .dropdown-menu.show {
      display: block;
    }
    .dropdown-menu div {
      padding: 0.65rem 1rem;
      cursor: pointer;
      font-size: 0.9rem;
      color: #333;
    }
    .dropdown-menu div:hover {
      background-color: #f7f7f7;
    }
    

  .menu-icon:hover,
.menu-dots:hover,
.sidebar-toggle:hover {
  background-color: #f0f0f0 !important;
}
    
  }
  
  }
  $11.8rem$2 font-weight: bold;
    font-weight: bold;
  }
  .menu-title {
    font-size: 2.175rem;
    font-weight: bold;
  }
</style>
</head>
<body>
  <div class="menu-bar">
    <button class="menu-icon" onclick="toggleSidebar()" aria-label="Toggle sidebar">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
        <line x1="9" y1="3" x2="9" y2="21"></line>
      </svg>
    </button>
    <div class="menu-title" id="pageTitle">All Notes</div>
    <button class="menu-dots" onclick="toggleDropdown()" aria-label="More options">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="5" r="1"></circle>
        <circle cx="12" cy="12" r="1"></circle>
        <circle cx="12" cy="19" r="1"></circle>
      </svg>
    </button>
    <div class="dropdown-menu" id="dropdownMenu">
      <div onclick="alert('Add note coming soon!')">＋ Add Note</div>
      <div>Settings</div>
      <div>Help</div>
      <div>Log out</div>
    </div>
  </div>

  <div class="app-container">
    <div class="sidebar" id="sidebar">
      <div class="sidebar-header">
        <span style="display: flex; align-items: center; gap: 0.5rem;">
          <img src="https://www.gravatar.com/avatar/?d=mp" alt="Profile" style="width: 32px; height: 32px; border-radius: 50%;">
          <span style="font-weight: bold; font-size: 1rem;">Zach</span>
        </span>
        <div style="display: flex; align-items: center; gap: 0.5rem;">
          <button class="menu-icon" aria-label="Settings">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="3"></circle>
              <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33h.09A1.65 1.65 0 0 0 9 3.6V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82v.09a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"></path>
            </svg>
          </button>
          <button class="sidebar-toggle" onclick="toggleSidebar()" aria-label="Close sidebar">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
              <line x1="9" y1="3" x2="9" y2="21"></line>
            </svg>
          </button>
          </div>
</div>
<ul>
  <li><a href="#" onclick="navigateTo('All Notes')">📝 All Notes</a></li>
        <li><a href="#" onclick="navigateTo('Starred')">⭐ Starred</a></li>
        <li style="margin-top: 1rem; font-size: 1.4rem; font-weight: bold; color: #888; display: flex; justify-content: space-between; align-items: center;">
          <span style="display: block; padding: 0.5rem 0.75rem;">FOLDERS</span>
          <button onclick="addFolder()" style="background: none; border: none; font-size: 1.1rem; cursor: pointer; color: #666;">＋</button>
        </li>
        <li><a href="#" onclick="navigateTo('Unsorted')">📂 Unsorted</a></li>
      </ul>
      <ul style="margin-top: auto;">
        <li><a href="#" onclick="navigateTo('Trash')">🗑️ Trash</a></li>
      </ul>
    </div>

    <main>
      <h2>All Notes</h2>
      <button onclick="addNote()" style="margin-bottom: 1rem; padding: 0.5rem 1rem; font-size: 1rem; background-color: #ededed; border: none; border-radius: 6px; cursor: pointer;">＋ Add Note</button>
      <p>This folder is currently empty.</p>
    </main>
  </div>

  <script>
    function toggleSidebar() {
      const sidebar = document.getElementById("sidebar");
      sidebar.classList.toggle("open");
    }

    function toggleDropdown() {
      const menu = document.getElementById("dropdownMenu");
      menu.classList.toggle("show");
    }

    window.addEventListener('click', function(e) {
      const sidebar = document.getElementById("sidebar");
      const button = document.querySelector('.menu-icon');
      const dots = document.querySelector('.menu-dots');
      const menu = document.getElementById("dropdownMenu");

      if (!button.contains(e.target) && !sidebar.contains(e.target) && window.innerWidth < 768) {
        sidebar.classList.remove("open");
      }

      if (!dots.contains(e.target) && !menu.contains(e.target)) {
        menu.classList.remove("show");
      }
    });

    function navigateTo(folderName) {
      const main = document.querySelector("main");
      main.innerHTML = `<h2>${folderName}</h2><p>This folder is currently empty.</p>`;
      document.getElementById('pageTitle').textContent = folderName;

      const sidebar = document.getElementById("sidebar");
      if (window.innerWidth < 768) {
        sidebar.classList.remove("open");
      }
    }

    function addFolder() {
      const icon = prompt("Choose an icon for your folder (e.g. 📁, 🧾, 📓):");
      if (!icon) return;

      const folderName = prompt("Enter a name for the new folder:");
      if (!folderName) return;

      const newItem = document.createElement("li");
      const newLink = document.createElement("a");
      newLink.href = "#";
      newLink.textContent = `${icon} ${folderName}`;
      newLink.onclick = function () {
        navigateTo(folderName);
        return false;
      };
      newItem.appendChild(newLink);

      const sidebarLists = document.querySelectorAll(".sidebar ul");
      const mainList = sidebarLists[0];
      const unsortedItem = mainList.querySelector("a[href='#'][onclick*='Unsorted']")?.parentNode;

      if (unsortedItem) {
        mainList.insertBefore(newItem, unsortedItem.nextSibling);
      } else {
        mainList.appendChild(newItem);
      }
    }
  
    function addNote() {
  const main = document.querySelector("main");
  main.innerHTML = `
    <h2>New Note</h2>
    <textarea style="width: 100%; height: 60vh; padding: 1rem; font-size: 1rem; border: 1px solid #ccc; border-radius: 6px; resize: vertical;">Start typing your note here...</textarea>
    <div style="margin-top: 1rem;">
      <button onclick="saveNote()" style="padding: 0.5rem 1.25rem; font-size: 1rem; background-color: #007aff; color: white; border: none; border-radius: 6px; cursor: pointer;">Save</button>
    </div>
  `;
  document.getElementById('pageTitle').textContent = "New Note";
}

function saveNote() {
  const textarea = document.querySelector("textarea");
  const noteText = textarea.value;
  alert("Note saved: " + noteText);
}
  </script>
</body>
</html>
